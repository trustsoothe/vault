export const ACCOUNT_CANNOT_BE_REMOVED = Object.freeze({
  name: "ACCOUNT_CANNOT_BE_REMOVED",
  message: "This account cannot be removed because it was not imported.",
});

export const INVALID_FILE_PASSWORD = Object.freeze({
  name: "INVALID_FILE_PASSWORD",
  message: "The password provided do not correspond to the portable wallet.",
});
