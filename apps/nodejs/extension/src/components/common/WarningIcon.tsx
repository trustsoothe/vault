import MuiWarningIcon from "@mui/icons-material/WarningAmber";
import React from "react";

const WarningIcon: React.FC = () => {
  return (
    <MuiWarningIcon
      sx={{
        color: "#ffb850",
        fontSize: 30,
      }}
    />
  );
};

export default WarningIcon;
